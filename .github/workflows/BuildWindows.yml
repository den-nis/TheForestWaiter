name: Build for windows

on:
  push:
    branches: [ pipeline ]
  pull_request:
    branches: [ pipeline ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x

    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet publish /p:Configuration=Release /p:PublishProfile=FolderProfile -o build
      
    - name: Remove debug files
      run: del ./build/*.pdb
      
    - name: Remove jsons
      run: del ./build/*.json
      
    - name: Publish-Artifacts
      uses: actions/upload-artifact@v2
      with:
        name: release.exe
        path:  ./build
        retention-days: 5
